/**
 * Rotas da aplicação Angular.
 * 
 * Define as rotas disponíveis para navegação na aplicação.
 * Atualmente a aplicação é single-page sem roteamento entre componentes.
 * 
 * @author API-IA Frontend
 * @version 1.0
 */
import { Routes } from '@angular/router';

/**
 * Array vazio de rotas.
 * 
 * Nesta implementação, toda a funcionalidade está concentrada no AppComponent
 * com sistema de abas (assistant, transcription, tts) em vez de navegação por rotas.
 */
export const routes: Routes = [];
